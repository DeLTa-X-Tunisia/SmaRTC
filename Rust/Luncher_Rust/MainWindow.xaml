<Window x:Class="Luncher_Rust.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="🚀 SmaRTC Rust Launcher - DeLTa-X Tunisia"
        Height="700" Width="900"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🦀 SmaRTC Rust Example" 
                       FontSize="32" FontWeight="Bold" 
                       Foreground="#FF4500"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Exemple de chat en Rust avec SignalR" 
                       FontSize="14" Foreground="Gray" 
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
            <TextBlock Text="© 2026 Mounir Azizi - DeLTa-X Tunisia 🇹🇳" 
                       FontSize="12" Foreground="#FF4500" 
                       HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="📦 cargo build" 
                    Command="{Binding BuildCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    Width="150" Height="45" Margin="5"
                    FontSize="14"/>
            
            <Button Content="▶️ Démarrer" 
                    Command="{Binding StartCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    Width="150" Height="45" Margin="5"
                    FontSize="14"
                    Background="#FF4500" Foreground="White"/>

            <Button Content="➕ Nouveau client" 
                    Command="{Binding NewClientCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    Width="150" Height="45" Margin="5"
                    FontSize="14"
                    Background="#B7410E" Foreground="White"/>
            
            <Button Content="⏹️ Arrêter tout" 
                    Command="{Binding StopCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    Width="150" Height="45" Margin="5"
                    FontSize="14"
                    Background="#E53935" Foreground="White"/>
        </StackPanel>

        <!-- Output Console -->
        <Border Grid.Row="2" 
                Background="#1E1E1E" 
                CornerRadius="8" 
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto"
                          x:Name="OutputScrollViewer">
                <TextBox Text="{Binding Output, Mode=OneWay}" 
                         IsReadOnly="True"
                         Background="Transparent"
                         Foreground="#FF4500"
                         FontFamily="Cascadia Code, Consolas, Courier New"
                         FontSize="13"
                         BorderThickness="0"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"/>
            </ScrollViewer>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Margin="0,15,0,0" Padding="10" Background="#2D2D30" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="{Binding StatusText}" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="1" 
                           Text="{Binding ClientCount, StringFormat='🦀 Clients Rust: {0}'}" 
                           Foreground="#FF4500" 
                           VerticalAlignment="Center"
                           Margin="0,0,20,0"/>
                
                <ui:ProgressRing Grid.Column="2" 
                                 IsActive="{Binding IsBusy}" 
                                 Width="24" Height="24"/>
            </Grid>
        </Border>
    </Grid>
</Window>
