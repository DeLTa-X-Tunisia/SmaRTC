<Window x:Class="Luncher_Kotlin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        Title="☕ SmaRTC Kotlin Launcher - DeLTa-X Tunisia 🇹🇳" 
        Height="750" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        ui:WindowHelper.UseModernWindowStyle="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header - Kotlin Purple/Orange Theme -->
        <Border Grid.Row="0" Padding="20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#7F52FF" Offset="0"/>
                    <GradientStop Color="#E44857" Offset="0.5"/>
                    <GradientStop Color="#F6A623" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🟣" FontSize="42" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="SmaRTC SDK - Kotlin Example" FontSize="24" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="SignalR Chat Client avec Microsoft SignalR pour Java/Kotlin" FontSize="12" Foreground="#FFE8E0" Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="2" Background="#40FFFFFF" CornerRadius="8" Padding="15,10" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="JDK 21" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text=" + " Foreground="#FFD4CC" FontSize="14"/>
                        <TextBlock Text="Kotlin 1.9" FontSize="14" Foreground="#F6A623" FontWeight="Bold"/>
                        <TextBlock Text=" + " Foreground="#FFD4CC" FontSize="14"/>
                        <TextBlock Text="SignalR" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Controls -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                
                <!-- Status Card -->
                <Border Background="#2A2040" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📊 État du Projet" FontSize="16" FontWeight="SemiBold" Foreground="#C4B4FF" Margin="0,0,0,15"/>
                        
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Java:" FontWeight="SemiBold" Width="80" Foreground="#A090D0"/>
                            <TextBlock x:Name="JavaStatusText" Grid.Column="1" Text="Vérification..." Foreground="#F6A623"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Gradle:" FontWeight="SemiBold" Width="80" Foreground="#A090D0"/>
                            <TextBlock x:Name="GradleStatusText" Grid.Column="1" Text="Non vérifié" Foreground="Gray"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Build:" FontWeight="SemiBold" Width="80" Foreground="#A090D0"/>
                            <TextBlock x:Name="BuildStatusText" Grid.Column="1" Text="Non compilé" Foreground="Gray"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Action Buttons -->
                <Button x:Name="VerifyButton" Content="🔍 Vérifier Java" 
                        HorizontalAlignment="Stretch" Height="45" Margin="0,0,0,10"
                        Click="VerifyButton_Click"
                        Background="#7F52FF" Foreground="White"/>
                
                <Button x:Name="BuildButton" Content="🔨 Build (Gradle)" 
                        HorizontalAlignment="Stretch" Height="45" Margin="0,0,0,10"
                        Background="#E44857" Foreground="White"
                        Click="BuildButton_Click"/>
                
                <Button x:Name="RunButton" Content="▶️ Exécuter Client #1" 
                        HorizontalAlignment="Stretch" Height="45" Margin="0,0,0,10"
                        Background="#F6A623" Foreground="White" FontWeight="Bold"
                        Click="RunButton_Click"/>

                <Button x:Name="RunButton2" Content="▶️ Exécuter Client #2" 
                        HorizontalAlignment="Stretch" Height="45" Margin="0,0,0,10"
                        Background="#C98B1D" Foreground="White"
                        Click="RunButton2_Click"/>

                <Button x:Name="StopAllButton" Content="⏹️ Arrêter Tout" 
                        HorizontalAlignment="Stretch" Height="45" Margin="0,0,0,10"
                        Background="#8B4049" Foreground="White"
                        Click="StopAllButton_Click"/>

                <Separator Margin="0,10" Background="#4A3A6A"/>

                <Button x:Name="CleanButton" Content="🧹 Clean" 
                        HorizontalAlignment="Stretch" Height="40" Margin="0,10,0,10"
                        Background="#5A4A7A" Foreground="White"
                        Click="CleanButton_Click"/>

                <Button x:Name="OpenFolderButton" Content="📂 Ouvrir le Dossier" 
                        HorizontalAlignment="Stretch" Height="40"
                        Background="#4A3A6A" Foreground="White"
                        Click="OpenFolderButton_Click"/>
                
                <!-- Info Card -->
                <Border CornerRadius="8" Padding="15" Margin="0,20,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#3D2060" Offset="0"/>
                            <GradientStop Color="#2D1845" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <TextBlock Text="💡 Architecture Kotlin" FontSize="14" FontWeight="SemiBold" Foreground="#F6A623" Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#B0A0D0">
                            • Microsoft SignalR Java Client&#x0a;
                            • Kotlin 1.9.22 + JVM&#x0a;
                            • Gradle 8.5 Build System&#x0a;
                            • JDK 21 LTS Runtime&#x0a;
                            • Jansi pour les couleurs
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Panel - Console Output -->
            <Border Grid.Column="1" CornerRadius="8" Padding="15">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#1E1830" Offset="0"/>
                        <GradientStop Color="#151020" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="📟 Console Output" FontSize="16" FontWeight="SemiBold" Foreground="#C4B4FF"/>
                        <Button x:Name="ClearButton" Content="🗑️ Effacer" 
                                HorizontalAlignment="Right" Padding="10,5"
                                Background="#4A3A6A" Foreground="White"
                                Click="ClearButton_Click"/>
                    </Grid>
                    
                    <ScrollViewer Grid.Row="1" x:Name="ConsoleScrollViewer" 
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ConsoleOutput" 
                                   FontFamily="Cascadia Code, Consolas, Courier New"
                                   FontSize="12" Foreground="#D0C0F0"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Padding="15,10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2A2040" Offset="0"/>
                    <GradientStop Color="#3D2050" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <TextBlock Text="© 2026 Mounir Azizi - DeLTa-X Tunisia 🇹🇳 - SmaRTC SDK Kotlin Example" 
                           VerticalAlignment="Center" FontSize="11" Foreground="#8070A0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Kotlin 1.9.22" FontSize="11" Foreground="#F6A623" FontWeight="SemiBold"/>
                    <TextBlock Text=" • " FontSize="11" Foreground="#6050A0"/>
                    <TextBlock Text="SignalR 8.0.0" FontSize="11" Foreground="#7F52FF"/>
                    <TextBlock Text=" • " FontSize="11" Foreground="#6050A0"/>
                    <TextBlock Text="JDK 21 LTS" FontSize="11" Foreground="#E44857"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
