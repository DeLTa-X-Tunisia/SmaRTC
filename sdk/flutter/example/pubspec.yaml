name: smartc_example
description: Example application for SmaRTC Flutter SDK
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # SmaRTC SDK
  smartc_sdk:
    path: ../
  
  # UI
  provider: ^6.1.1
  
  # Utils
  permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
